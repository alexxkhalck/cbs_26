[project]
name = "test-poetry"
version = "0.1.0"
description = ""
authors = [
    {name = "alexxkhalck",email = "khalturinalexx@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django (==5.2)",
    "beautifulsoup4 (>=4.15.0,<5.0.0)",
    "pillow (==12.3)"
]

[tool.poetry]
packages = [{include = "test_poetry", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
